services:
  - type: web
    name: rankingATD
    runtime: php
    rootDirectory: ./pagina-web
    buildCommand: echo "PHP app - No build required"
    startCommand: php -S 0.0.0.0:$PORT
    envVars:
      - key: PHP_VERSION
        value: 8.2
